services:
  - type: web
    name: privguard-ai
    runtime: python
    rootDir: Techathon
    buildCommand: "./build.sh"
    startCommand: "python privguard.py --server --host 0.0.0.0 --port $PORT"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
